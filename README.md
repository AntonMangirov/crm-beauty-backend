# CRM Beauty Backend

Backend API для системы управления салоном красоты, построенный на Node.js, Express, TypeScript и Prisma.

## 🚀 Технологический стек

- **Node.js** - серверная платформа
- **Express.js** - веб-фреймворк
- **TypeScript** - типизированный JavaScript
- **Prisma** - ORM для работы с базой данных
- **PostgreSQL** - реляционная база данных
- **JWT** - аутентификация
- **bcrypt** - хеширование паролей
- **Zod** - валидация данных
- **CORS** - настройка CORS
- **dotenv** - управление переменными окружения

## 📁 Структура проекта

```
src/
├── app.ts          # Express приложение (без сервера)
├── index.ts        # Точка входа - запуск сервера
├── prismaClient.ts # Настройка Prisma Client
├── controllers/    # Контроллеры для обработки запросов
├── routes/         # Маршруты API
├── middleware/     # Middleware функции
├── types/          # TypeScript типы
├── utils/          # Утилиты
└── prisma/         # Схема базы данных и миграции
```

## 🛠 Установка и настройка

### Предварительные требования

- Node.js (версия 18 или выше)
- npm или yarn
- PostgreSQL
- Docker (опционально)

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd crm-beauty-backend
```

### 2. Установка зависимостей

```bash
npm install
```

### 3. Настройка базы данных

#### Вариант A: Использование Docker (рекомендуется)

```bash
# Запуск PostgreSQL контейнера
docker run --name crm-postgres \
  -e POSTGRES_PASSWORD=12345 \
  -e POSTGRES_USER=postgres \
  -e POSTGRES_DB=crm_beauty_db \
  -p 5433:5432 \
  -d postgres:15

# Проверка статуса контейнера
docker ps

# Подключение к базе данных (опционально)
docker exec -it crm-postgres psql -U postgres -d crm_beauty_db
```

#### Вариант B: Локальная установка PostgreSQL

Установите PostgreSQL локально и создайте базу данных `crm_beauty_db`.

### 4. Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```env
DATABASE_URL="postgresql://postgres:12345@localhost:5433/crm_beauty_db"
JWT_SECRET="your-jwt-secret-key-change-in-production"
PORT=3000
NODE_ENV=development
```

### 5. Инициализация базы данных

```bash
# Генерация Prisma клиента
npx prisma generate

# Применение миграций
npx prisma migrate dev

# Заполнение базы тестовыми данными (рекомендуется)
npm run seed
```

#### Альтернативные команды для seed:

```bash
# Через Prisma
npx prisma db seed

# Прямой запуск TypeScript файла
npx ts-node prisma/seed.ts
```

## 🚀 Запуск приложения

### Быстрый старт для новых разработчиков

```bash
# 1. Установка зависимостей
npm install

# 2. Настройка базы данных (создайте .env файл)
# DATABASE_URL="postgresql://postgres:password@localhost:5432/crm_beauty?schema=public"
# JWT_SECRET="your-jwt-secret-key"

# 3. Применение миграций
npm run db:migrate

# 4. Заполнение тестовыми данными
npm run seed

# 5. Запуск сервера
npm run dev
```

### Режим разработки

```bash
npm run dev
```

### Режим продакшена

```bash
npm run build
npm start
```

## 📝 Доступные скрипты

- `npm run dev` - запуск в режиме разработки с hot reload
- `npm run build` - сборка проекта
- `npm start` - запуск собранного приложения
- `npm run lint` - проверка кода линтером
- `npm run lint:fix` - автоматическое исправление ошибок линтера
- `npm run format` - форматирование кода с помощью Prettier
- `npm run db:generate` - генерация Prisma клиента
- `npm run db:migrate` - применение миграций
- `npm run db:seed` - заполнение базы тестовыми данными через Prisma
- `npm run seed` - запуск seed-скрипта (рекомендуется)
- `npm run db:studio` - открыть Prisma Studio
- `npm run db:reset` - сброс базы данных

## 🧪 Тестирование

### Запуск тестов

```bash
# Запуск всех тестов
npm test

# Запуск тестов в режиме watch
npm run test:watch

# Запуск тестов с покрытием
npm run test:coverage
```

### Тестирование API

```bash
# Проверка здоровья сервера
curl http://localhost:3000/api/health

# Проверка подключения к БД
curl http://localhost:3000/api/db/status

# Получение пользователей
curl http://localhost:3000/api/users

# Получение услуг
curl http://localhost:3000/api/services
```

## 🗄 Работа с базой данных

### Prisma команды

```bash
# Просмотр базы данных в Prisma Studio
npx prisma studio

# Создание новой миграции
npx prisma migrate dev --name migration_name

# Сброс базы данных
npx prisma migrate reset

# Применение миграций в продакшене
npx prisma migrate deploy
```

### 🌱 Seed-скрипт

Проект включает в себя seed-скрипт для заполнения базы данных тестовыми данными.

#### Что создает seed-скрипт:

**👩‍💅 Анна Красоткина** (anna@example.com)

- Специалист по маникюру
- 4 услуги: классический маникюр, маникюр + гель-лак, аппаратный маникюр, френч

**👩‍💼 Мария Стильная** (maria@example.com)

- Мастер широкого профиля
- 5 услуг: педикюр, наращивание ногтей, коррекция и др.

#### Запуск seed-скрипта:

```bash
# Основная команда
npm run seed

# Альтернативные способы
npx prisma db seed
npx ts-node prisma/seed.ts
```

#### Данные для входа:

- **Email:** anna@example.com или maria@example.com
- **Пароль:** password123

#### Просмотр созданных данных:

```bash
# Открыть Prisma Studio для просмотра данных
npm run db:studio
```

### 🔄 Миграции

#### Создание новой миграции:

```bash
# Создание миграции с автоматическим именем
npx prisma migrate dev

# Создание миграции с кастомным именем
npx prisma migrate dev --name add_new_table
```

#### Применение миграций:

```bash
# В разработке (создает и применяет)
npx prisma migrate dev

# В продакшене (только применяет)
npx prisma migrate deploy
```

#### Сброс базы данных:

```bash
# Полный сброс с пересозданием БД
npx prisma migrate reset

# Сброс с применением seed-скрипта
npx prisma migrate reset --force
```

## 🔧 Настройка инструментов разработки

### ESLint + Prettier

Проект настроен с ESLint и Prettier для обеспечения качества кода:

```bash
# Установка зависимостей для линтинга
npm install -D eslint prettier eslint-config-prettier eslint-plugin-prettier

# Инициализация ESLint
npx eslint --init
```

### Husky + lint-staged

Настроены git hooks для автоматической проверки кода:

```bash
# Установка Husky и lint-staged
npm install -D husky lint-staged

# Инициализация Husky
npx husky install

# Добавление pre-commit hook
echo "npx lint-staged" > .husky/pre-commit
```

## 📚 API Документация

API документация будет доступна по адресу `/api/docs` после запуска приложения.

### Основные эндпоинты

#### Системные

- `GET /api/health` - проверка состояния сервера
- `GET /api/db/status` - проверка подключения к базе данных

#### Аутентификация

- `POST /api/auth/login` - аутентификация пользователя
- `POST /api/auth/register` - регистрация нового пользователя
- `GET /api/auth/me` - получение информации о текущем пользователе

#### Пользователи

- `GET /api/users` - получение списка пользователей

#### Услуги (требуют авторизации)

- `GET /api/services` - получение услуг мастера
- `POST /api/services` - создание новой услуги
- `GET /api/services/:id` - получение услуги по ID
- `PATCH /api/services/:id` - обновление услуги
- `DELETE /api/services/:id` - удаление услуги

#### Записи

- `GET /api/appointments` - получение списка записей
- `POST /api/appointments` - создание новой записи

### 🔐 Тестовые данные

После запуска seed-скрипта доступны следующие тестовые аккаунты:

#### Мастера для тестирования:

**👩‍💅 Анна Красоткина**

- Email: `anna@example.com`
- Пароль: `password123`
- Услуги: 4 (маникюр, гель-лак, аппаратный, френч)

**👩‍💼 Мария Стильная**

- Email: `maria@example.com`
- Пароль: `password123`
- Услуги: 5 (педикюр, наращивание, коррекция и др.)

#### Примеры API запросов:

```bash
# Авторизация
curl -X POST http://localhost:3000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email": "anna@example.com", "password": "password123"}'

# Получение услуг (с токеном)
curl -X GET http://localhost:3000/api/services \
  -H "Authorization: Bearer YOUR_TOKEN"

# Создание услуги
curl -X POST http://localhost:3000/api/services \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"name": "Новая услуга", "price": 2000, "durationMin": 90}'
```

## 🧪 Тестирование

```bash
# Запуск всех тестов
npm test

# Запуск тестов в режиме watch
npm run test:watch

# Запуск тестов с покрытием
npm run test:coverage
```

## 🚀 Деплой

### Docker

```bash
# Сборка Docker образа
docker build -t crm-beauty-backend .

# Запуск контейнера
docker run -p 3000:3000 crm-beauty-backend
```

### Переменные окружения для продакшена

```env
NODE_ENV=production
DATABASE_URL=postgresql://user:password@host:port/database
JWT_SECRET=your-production-jwt-secret
PORT=3000
```

## 🤝 Участие в разработке

1. Форкните репозиторий
2. Создайте ветку для новой функции (`git checkout -b feature/amazing-feature`)
3. Зафиксируйте изменения (`git commit -m 'Add some amazing feature'`)
4. Отправьте в ветку (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

## 📄 Лицензия

Этот проект лицензирован под MIT License.

## 📞 Поддержка

Если у вас есть вопросы или проблемы, создайте issue в репозитории или свяжитесь с командой разработки.
