# Полный аудит совместимости обновлений расписания

## Обзор изменений

Добавлены новые поля в модель `User`:
- `workSchedule` (JSONB) - расписание работы по дням недели
- `breaks` (JSONB) - перерывы мастера
- `defaultBufferMinutes` (Int) - буфер после услуги
- `slotStepMinutes` (Int) - шаг генерации слотов

## Анализ эндпоинтов

### 1. Эндпоинты, которые читают расписание

#### ✅ GET `/api/public/:slug/timeslots` - `getTimeslots`
**Статус**: ✅ Безопасен, но требует внимания

**Текущая реализация**:
- Загружает `workSchedule`, `breaks`, `defaultBufferMinutes`, `slotStepMinutes` из БД
- Использует fallback значения если поля `null`
- Преобразует расписание в формат для `calculateAvailableSlots`

**Риски**:
- ⚠️ **Низкий риск**: Если `workSchedule` содержит некорректный JSON → fallback на дефолтное расписание
- ⚠️ **Низкий риск**: Если `workSchedule` не содержит расписание для дня недели → fallback на дефолтное расписание
- ⚠️ **Средний риск**: Если `workSchedule` содержит некорректную структуру (не массив, не объекты) → может упасть с ошибкой

**Проблемы**:
1. Нет проверки типа `workSchedule` - если это не массив, код упадет
2. Нет проверки структуры `intervals` - если формат неверный, код упадет
3. Нет валидации формата времени в `from`/`to` - может быть любая строка

**Рекомендации**:
```typescript
// Добавить проверку типа
if (!Array.isArray(master.workSchedule)) {
  // Логировать ошибку, использовать fallback
  workIntervals = [{ start: '09:00', end: '18:00' }];
}
```

#### ✅ GET `/api/me` - `getMe`
**Статус**: ✅ Безопасен

**Текущая реализация**:
- Использует `select` с явным списком полей
- **НЕ включает** новые поля (`workSchedule`, `breaks`, `defaultBufferMinutes`, `slotStepMinutes`)
- Схема `MeResponseSchema` не включает новые поля

**Риски**: ✅ Нет рисков - новые поля не возвращаются

**Проблемы**: Нет

**Рекомендации**: 
- Опционально: добавить новые поля в ответ (если фронтенд будет их использовать)

#### ✅ GET `/api/public/:slug` - `getPublicProfileBySlug`
**Статус**: ✅ Безопасен

**Текущая реализация**:
- Использует `select` с явным списком полей
- **НЕ включает** новые поля
- Схема `PublicProfileResponseSchema` не включает новые поля

**Риски**: ✅ Нет рисков

**Проблемы**: Нет

---

### 2. Эндпоинты, которые создают бронирования

#### ⚠️ POST `/api/public/:slug/book` - `bookPublicSlot`
**Статус**: ⚠️ Требует внимания

**Текущая реализация**:
- Использует старую валидацию `isWorkingHours` и `isWorkingDay` из `BookingRequestSchema`
- Проверяет время в UTC (9:00-18:00 UTC)
- **НЕ использует** новое расписание мастера для валидации

**Риски**:
- 🔴 **Высокий риск**: Валидация времени не учитывает новое расписание мастера
- 🔴 **Высокий риск**: Можно создать бронирование вне рабочих часов мастера
- 🔴 **Высокий риск**: Можно создать бронирование в перерывы мастера
- 🔴 **Высокий риск**: Можно создать бронирование в выходные дни мастера (если мастер работает в выходные)

**Проблемы**:
1. **Критическая**: Валидация использует жестко заданные часы (9:00-18:00 UTC) вместо расписания мастера
2. **Критическая**: Не проверяет перерывы мастера
3. **Критическая**: Не проверяет рабочие дни мастера (использует общую логику пн-пт)
4. **Средняя**: Не учитывает часовой пояс мастера при валидации

**Пример проблемы**:
```typescript
// Мастер работает 10:00-17:00 MSK (07:00-14:00 UTC)
// Клиент пытается записаться на 09:00 UTC (12:00 MSK)
// Старая валидация: ✅ проходит (9:00-18:00 UTC)
// Реальность: ❌ мастер еще не работает (работает с 10:00 MSK)
```

**Рекомендации**:
1. **Критично**: Обновить валидацию в `BookingRequestSchema` для использования расписания мастера
2. Создать функцию `isValidBookingTimeForMaster(date, masterId)` которая:
   - Загружает расписание мастера
   - Проверяет день недели
   - Проверяет рабочие интервалы
   - Проверяет перерывы
   - Учитывает часовой пояс мастера

---

### 3. Эндпоинты, которые выводят расписание в админке

#### ✅ GET `/api/me` - `getMe`
**Статус**: ✅ Безопасен, но не возвращает расписание

**Текущая реализация**:
- Не возвращает расписание мастера
- Фронтенд не получает информацию о расписании

**Риски**: ✅ Нет рисков, но функционал отсутствует

**Проблемы**:
- Фронтенд не может отобразить расписание мастера в админке

**Рекомендации**:
- Добавить эндпоинт `GET /api/me/schedule` для получения расписания
- Или добавить поля в `GET /api/me` (опционально)

#### ✅ PUT `/api/me/schedule` - `updateSchedule`
**Статус**: ✅ Безопасен

**Текущая реализация**:
- Валидация через Zod
- Обновляет только переданные поля
- Возвращает обновленное расписание

**Риски**: ✅ Нет рисков

**Проблемы**: Нет

---

### 4. Эндпоинты, которые возвращают список услуг

#### ✅ GET `/api/public/:slug` - `getPublicProfileBySlug`
**Статус**: ✅ Безопасен

**Текущая реализация**:
- Возвращает список услуг мастера
- Не использует расписание для фильтрации

**Риски**: ✅ Нет рисков

**Проблемы**: Нет

#### ✅ GET `/api/me/services` - `getServices`
**Статус**: ✅ Безопасен

**Текущая реализация**:
- Возвращает список услуг мастера
- Не связан с расписанием

**Риски**: ✅ Нет рисков

**Проблемы**: Нет

---

## Анализ валидации времени

### Проблема 1: Старая валидация в `BookingRequestSchema`

**Файл**: `src/schemas/public.ts`

**Текущая валидация**:
```typescript
.refine(date => isWorkingHours(date), {
  message: 'Время записи должно быть в рабочих часах (9:00 - 18:00 UTC)',
})
.refine(date => isWorkingDay(date), {
  message: 'Запись возможна только в рабочие дни (понедельник - пятница)',
})
```

**Проблемы**:
1. 🔴 Использует жестко заданные часы (9:00-18:00 UTC)
2. 🔴 Не учитывает расписание мастера
3. 🔴 Не учитывает перерывы мастера
4. 🔴 Не учитывает часовой пояс мастера
5. 🔴 Использует общую логику рабочих дней (пн-пт)

**Риски**:
- Можно создать бронирование вне рабочих часов мастера
- Можно создать бронирование в перерывы мастера
- Можно создать бронирование в выходные (если мастер работает в выходные)

**Решение**:
Создать новую функцию валидации, которая учитывает расписание мастера:
```typescript
async function isValidBookingTimeForMaster(
  date: Date,
  masterId: string
): Promise<{ valid: boolean; reason?: string }> {
  // Загрузить расписание мастера
  // Проверить день недели
  // Проверить рабочие интервалы
  // Проверить перерывы
  // Учесть часовой пояс
}
```

---

### Проблема 2: Валидация в `bookPublicSlot`

**Файл**: `src/controllers/publicController.ts`

**Текущая реализация**:
- Использует валидацию из `BookingRequestSchema` (старая логика)
- Проверяет конфликты через `overlapping` запрос
- **НЕ проверяет** расписание мастера

**Риски**:
- 🔴 Можно создать бронирование вне рабочих часов
- 🔴 Можно создать бронирование в перерывы
- 🔴 Можно создать бронирование в выходные

**Решение**:
Добавить проверку расписания перед созданием бронирования:
```typescript
// После загрузки мастера и услуги
const masterSchedule = await getMasterScheduleForDate(master.id, start);
if (!isTimeInMasterSchedule(start, masterSchedule)) {
  return res.status(400).json({
    error: 'Time not available',
    message: 'Выбранное время не входит в рабочие часы мастера',
  });
}
```

---

## Анализ таймзон

### Проблема 1: Смешение UTC и локального времени

**Текущая реализация**:
- `getTimeslots` использует часовой пояс мастера для преобразования расписания
- `bookPublicSlot` использует UTC для валидации
- `BookingRequestSchema` валидирует в UTC

**Риски**:
- ⚠️ **Средний риск**: Несоответствие между генерацией слотов и валидацией бронирования
- ⚠️ **Средний риск**: Клиент может увидеть слоты в одном часовом поясе, но валидация проходит в другом

**Пример проблемы**:
```
Мастер: Москва (UTC+3)
Расписание: 10:00-18:00 MSK (07:00-15:00 UTC)

Клиент видит слоты: 10:00, 11:00, ... 18:00 MSK
Клиент выбирает: 10:00 MSK → отправляет 07:00 UTC
Валидация: проверяет 07:00 UTC → ✅ проходит (9:00-18:00 UTC)
Результат: ✅ Бронирование создается

НО: Если мастер работает 10:00-17:00 MSK (07:00-14:00 UTC)
Клиент видит слоты: 10:00-17:00 MSK
Клиент выбирает: 17:00 MSK → отправляет 14:00 UTC
Валидация: проверяет 14:00 UTC → ✅ проходит (9:00-18:00 UTC)
Результат: ✅ Бронирование создается (но мастер уже не работает!)
```

**Решение**:
1. Унифицировать использование часового пояса
2. Валидировать время с учетом часового пояса мастера
3. Преобразовывать время клиента в локальное время мастера перед валидацией

---

### Проблема 2: Преобразование времени в `slotCalculator`

**Текущая реализация**:
- Функция `localTimeToUTC` использует итеративный подход
- Может быть неточной при переходе на летнее/зимнее время
- Использует `Intl.DateTimeFormat` для преобразования

**Риски**:
- ⚠️ **Низкий риск**: Неточность при переходе на летнее/зимнее время
- ⚠️ **Низкий риск**: Проблемы с часовыми поясами без перехода на летнее время

**Рекомендации**:
- Использовать библиотеку `date-fns-tz` или `luxon` для более точного преобразования
- Или улучшить алгоритм преобразования

---

## Анализ работы с null значениями

### Проблема 1: Фронтенд не получает расписание

**Текущая реализация**:
- `GET /api/me` не возвращает расписание
- Фронтенд не знает о расписании мастера

**Риски**:
- ✅ **Нет рисков** - фронтенд не использует расписание
- ⚠️ **Функциональный риск**: Фронтенд не может отобразить расписание

**Проблемы**:
- Фронтенд не может показать расписание мастера в админке
- Фронтенд не может валидировать время на клиенте

**Рекомендации**:
- Добавить эндпоинт `GET /api/me/schedule`
- Или добавить поля в `GET /api/me` (опционально)

---

### Проблема 2: Обработка null в `getTimeslots`

**Текущая реализация**:
```typescript
if (master.workSchedule && typeof master.workSchedule === 'object') {
  // Парсинг
}
if (workIntervals.length === 0) {
  workIntervals = [{ start: '09:00', end: '18:00' }]; // Fallback
}
```

**Риски**:
- ⚠️ **Средний риск**: Если `workSchedule` не массив → код упадет
- ⚠️ **Средний риск**: Если структура неверная → код упадет

**Проблемы**:
1. Нет проверки `Array.isArray(master.workSchedule)`
2. Нет проверки структуры `intervals`
3. Нет проверки формата времени `from`/`to`

**Рекомендации**:
```typescript
// Безопасная обработка
let workIntervals: WorkInterval[] = [];
if (master.workSchedule && Array.isArray(master.workSchedule)) {
  try {
    const schedule = master.workSchedule as Array<{
      dayOfWeek: number;
      intervals: Array<{ from: string; to: string }>;
    }>;
    
    const daySchedule = schedule.find(s => s.dayOfWeek === dayOfWeek);
    if (daySchedule && Array.isArray(daySchedule.intervals)) {
      workIntervals = daySchedule.intervals
        .filter(interval => 
          interval.from && 
          interval.to && 
          /^\d{2}:\d{2}$/.test(interval.from) &&
          /^\d{2}:\d{2}$/.test(interval.to)
        )
        .map(interval => ({
          start: interval.from,
          end: interval.to,
        }));
    }
  } catch (error) {
    logError('Ошибка парсинга workSchedule', error);
    // Используем fallback
  }
}

if (workIntervals.length === 0) {
  workIntervals = [{ start: '09:00', end: '18:00' }];
}
```

---

## Список проблем

### Критические проблемы

1. 🔴 **Валидация времени в `BookingRequestSchema` не учитывает расписание мастера**
   - Использует жестко заданные часы (9:00-18:00 UTC)
   - Не проверяет перерывы мастера
   - Не проверяет рабочие дни мастера
   - Не учитывает часовой пояс мастера

2. 🔴 **`bookPublicSlot` не проверяет расписание мастера**
   - Можно создать бронирование вне рабочих часов
   - Можно создать бронирование в перерывы
   - Можно создать бронирование в выходные

3. 🔴 **Несоответствие между генерацией слотов и валидацией бронирования**
   - Слоты генерируются с учетом расписания
   - Валидация использует старую логику
   - Клиент может увидеть слоты, но не сможет забронировать (или наоборот)

### Средние проблемы

4. ⚠️ **Нет проверки типа `workSchedule` в `getTimeslots`**
   - Если это не массив → код упадет
   - Нужна проверка `Array.isArray()`

5. ⚠️ **Нет валидации структуры `workSchedule`**
   - Нет проверки формата времени
   - Нет проверки структуры `intervals`

6. ⚠️ **Фронтенд не получает расписание**
   - Не может отобразить расписание в админке
   - Не может валидировать время на клиенте

7. ⚠️ **Преобразование времени может быть неточным**
   - Итеративный подход может давать ошибки
   - Проблемы с переходом на летнее/зимнее время

### Низкие проблемы

8. ⚠️ **Нет эндпоинта для получения расписания**
   - Фронтенд не может загрузить расписание
   - Нужен `GET /api/me/schedule`

9. ⚠️ **Нет поля `autoBuffer` в БД**
   - Используется значение по умолчанию `false`
   - Нужно добавить поле в схему

---

## План исправления

### Приоритет 1: Критические проблемы

#### 1.1. Обновить валидацию времени в `BookingRequestSchema`

**Файл**: `src/schemas/public.ts**

**Действия**:
1. Создать функцию `isValidBookingTimeForMaster(date, masterId)` которая:
   - Загружает расписание мастера
   - Проверяет день недели
   - Проверяет рабочие интервалы
   - Проверяет перерывы
   - Учитывает часовой пояс мастера

2. Обновить `BookingRequestSchema`:
   ```typescript
   .refine(async (data) => {
     // Загрузить мастера по slug из params
     // Проверить время с учетом расписания
     return await isValidBookingTimeForMaster(data.startAt, masterId);
   }, {
     message: 'Время записи не входит в рабочие часы мастера',
   })
   ```

**Проблема**: Zod не поддерживает асинхронные refine напрямую

**Альтернативное решение**:
- Убрать валидацию из схемы
- Добавить проверку в контроллер `bookPublicSlot`

#### 1.2. Добавить проверку расписания в `bookPublicSlot`

**Файл**: `src/controllers/publicController.ts`

**Действия**:
1. После загрузки мастера и услуги добавить проверку:
   ```typescript
   // Загрузить расписание мастера
   const masterSchedule = await getMasterScheduleForDate(master.id, start);
   
   // Проверить день недели
   const dayOfWeek = start.getUTCDay();
   const daySchedule = masterSchedule.workSchedule?.find(s => s.dayOfWeek === dayOfWeek);
   
   if (!daySchedule || daySchedule.intervals.length === 0) {
     return res.status(400).json({
       error: 'Time not available',
       message: 'Мастер не работает в этот день',
     });
   }
   
   // Проверить рабочие интервалы
   const isInWorkInterval = daySchedule.intervals.some(interval => {
     // Преобразовать время в локальное время мастера
     // Проверить попадание в интервал
   });
   
   if (!isInWorkInterval) {
     return res.status(400).json({
       error: 'Time not available',
       message: 'Выбранное время не входит в рабочие часы мастера',
     });
   }
   
   // Проверить перерывы
   const isInBreak = masterSchedule.breaks?.some(breakItem => {
     // Проверить попадание в перерыв
   });
   
   if (isInBreak) {
     return res.status(400).json({
       error: 'Time not available',
       message: 'Выбранное время попадает на перерыв мастера',
     });
   }
   ```

#### 1.3. Унифицировать использование часового пояса

**Действия**:
1. Всегда использовать часовой пояс мастера для валидации
2. Преобразовывать время клиента в локальное время мастера перед проверкой
3. Использовать одну и ту же логику преобразования везде

---

### Приоритет 2: Средние проблемы

#### 2.1. Добавить безопасную обработку `workSchedule`

**Файл**: `src/controllers/publicController.ts`

**Действия**:
```typescript
let workIntervals: WorkInterval[] = [];
if (master.workSchedule && Array.isArray(master.workSchedule)) {
  try {
    const schedule = master.workSchedule as Array<{
      dayOfWeek: number;
      intervals: Array<{ from: string; to: string }>;
    }>;
    
    const daySchedule = schedule.find(s => s.dayOfWeek === dayOfWeek);
    if (daySchedule && Array.isArray(daySchedule.intervals)) {
      workIntervals = daySchedule.intervals
        .filter(interval => 
          interval?.from && 
          interval?.to && 
          /^\d{2}:\d{2}$/.test(interval.from) &&
          /^\d{2}:\d{2}$/.test(interval.to)
        )
        .map(interval => ({
          start: interval.from,
          end: interval.to,
        }));
    }
  } catch (error) {
    logError('Ошибка парсинга workSchedule', error, {
      masterId: master.id,
      workSchedule: master.workSchedule,
    });
  }
}

if (workIntervals.length === 0) {
  workIntervals = [{ start: '09:00', end: '18:00' }];
}
```

#### 2.2. Добавить эндпоинт для получения расписания

**Файл**: `src/controllers/meController.ts`

**Действия**:
```typescript
export async function getSchedule(req: Request, res: Response) {
  const userId = req.user?.id;
  const master = await prisma.user.findUnique({
    where: { id: userId },
    select: {
      workSchedule: true,
      breaks: true,
      defaultBufferMinutes: true,
      slotStepMinutes: true,
    },
  });
  
  return res.json({
    workSchedule: master.workSchedule,
    breaks: master.breaks,
    defaultBufferMinutes: master.defaultBufferMinutes,
    slotStepMinutes: master.slotStepMinutes,
  });
}
```

**Роут**: `GET /api/me/schedule`

#### 2.3. Улучшить преобразование времени

**Действия**:
- Использовать библиотеку `date-fns-tz` для более точного преобразования
- Или улучшить алгоритм `localTimeToUTC`

---

### Приоритет 3: Низкие проблемы

#### 3.1. Добавить поле `autoBuffer` в БД

**Действия**:
1. Создать миграцию для добавления поля `autoBuffer Boolean @default(false)`
2. Обновить контроллер для загрузки `autoBuffer` из БД

---

## Риски при неправильном расписании

### Риск 1: Некорректный JSON в `workSchedule`

**Симптомы**:
- Ошибка парсинга JSON
- Код падает с ошибкой

**Последствия**:
- Эндпоинт `/api/public/:slug/timeslots` возвращает 500
- Клиенты не могут увидеть доступные слоты

**Защита**:
- ✅ Есть fallback на дефолтное расписание
- ⚠️ Нужна проверка типа перед парсингом

---

### Риск 2: Некорректная структура `workSchedule`

**Симптомы**:
- `workSchedule` не массив
- Отсутствует поле `intervals`
- Неверный формат времени

**Последствия**:
- Код падает при обращении к `schedule.find()`
- Код падает при обращении к `daySchedule.intervals`

**Защита**:
- ⚠️ Нужна проверка типа и структуры

---

### Риск 3: Перерывы вне рабочих интервалов

**Симптомы**:
- Перерывы не учитываются при генерации слотов
- Слоты генерируются в перерывы

**Последствия**:
- Клиенты видят слоты в перерывы
- Можно забронировать время в перерыв

**Защита**:
- ✅ Валидация в `UpdateScheduleSchema` проверяет, что перерывы внутри интервалов
- ⚠️ Но если данные уже в БД некорректные, валидация не поможет

---

### Риск 4: Несоответствие часовых поясов

**Симптомы**:
- Клиент видит слоты в одном часовом поясе
- Валидация проходит в другом часовом поясе
- Бронирование создается вне рабочих часов

**Последствия**:
- Клиент может забронировать время, когда мастер не работает
- Или наоборот - не может забронировать доступное время

**Защита**:
- ⚠️ Нужна унификация использования часового пояса

---

## Проверка фронтенда

### Фронтенд не получает расписание

**Текущее состояние**:
- `GET /api/me` не возвращает расписание
- Фронтенд не знает о расписании мастера

**Риски**:
- ✅ **Нет рисков** - фронтенд не использует расписание
- Фронтенд не может отобразить расписание в админке
- Фронтенд не может валидировать время на клиенте

**Проверка**:
- ✅ `MeResponse` интерфейс не включает расписание
- ✅ Фронтенд не обращается к полям расписания
- ✅ Фронтенд не сломается при `workSchedule = null`

---

## Итоговый список проблем

### Критические (требуют немедленного исправления)

1. 🔴 Валидация времени в `BookingRequestSchema` не учитывает расписание мастера
2. 🔴 `bookPublicSlot` не проверяет расписание мастера перед созданием бронирования
3. 🔴 Несоответствие между генерацией слотов и валидацией бронирования

### Средние (требуют исправления в ближайшее время)

4. ⚠️ Нет проверки типа `workSchedule` в `getTimeslots`
5. ⚠️ Нет валидации структуры `workSchedule`
6. ⚠️ Фронтенд не получает расписание
7. ⚠️ Преобразование времени может быть неточным

### Низкие (можно исправить позже)

8. ⚠️ Нет эндпоинта для получения расписания
9. ⚠️ Нет поля `autoBuffer` в БД

---

## План исправления (приоритетный порядок)

### Этап 1: Критические исправления

1. **Добавить проверку расписания в `bookPublicSlot`**
   - Создать функцию `isValidBookingTimeForMaster`
   - Добавить проверку перед созданием бронирования
   - Учесть часовой пояс мастера

2. **Обновить валидацию времени**
   - Убрать жестко заданные часы из `BookingRequestSchema`
   - Добавить проверку в контроллер

3. **Унифицировать использование часового пояса**
   - Всегда использовать часовой пояс мастера
   - Преобразовывать время клиента в локальное время мастера

### Этап 2: Средние исправления

4. **Добавить безопасную обработку `workSchedule`**
   - Проверка типа и структуры
   - Обработка ошибок

5. **Добавить эндпоинт для получения расписания**
   - `GET /api/me/schedule`

6. **Улучшить преобразование времени**
   - Использовать библиотеку или улучшить алгоритм

### Этап 3: Низкие исправления

7. **Добавить поле `autoBuffer` в БД**
   - Создать миграцию
   - Обновить контроллер

---

## Заключение

### ✅ Безопасные изменения

- Добавление новых полей в БД (обратно совместимо)
- Генерация слотов с учетом расписания (fallback на дефолты)
- Эндпоинт обновления расписания (новый функционал)

### ⚠️ Требуют внимания

- Валидация времени при создании бронирования (критично)
- Обработка некорректных данных в `workSchedule` (средне)
- Унификация использования часового пояса (средне)

### 🔴 Критические проблемы

- Можно создать бронирование вне рабочих часов мастера
- Можно создать бронирование в перерывы мастера
- Несоответствие между генерацией слотов и валидацией

**Рекомендация**: Исправить критические проблемы перед деплоем в продакшн.














