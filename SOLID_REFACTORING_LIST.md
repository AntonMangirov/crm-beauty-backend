# Список компонентов для рефакторинга по принципам SOLID

## 🔴 Критические нарушения (высокий приоритет)

### 1. **meController.ts** (2152 строки)

**Проблемы:**

- ❌ **SRP (Single Responsibility Principle)**: Один контроллер управляет 15+ различными сущностями:
  - Профиль мастера
  - Записи (appointments)
  - Клиенты
  - Фото записей
  - Аналитика
  - Портфолио
  - Расписание
  - Пароль/Email/Телефон
- ❌ **DIP (Dependency Inversion Principle)**: Прямое использование `prisma` вместо абстракции
- ❌ **OCP (Open/Closed Principle)**: Сложно расширять функциональность без изменения существующего кода

**Рекомендации:**

- Разделить на отдельные контроллеры:
  - `ProfileController` - управление профилем
  - `AppointmentController` - управление записями
  - `ClientController` - управление клиентами
  - `PhotoController` - управление фото
  - `AnalyticsController` - аналитика
  - `PortfolioController` - портфолио
  - `ScheduleController` - расписание
  - `AccountController` - пароль/email/телефон
- Создать сервисный слой для бизнес-логики
- Использовать репозитории для работы с БД

+++++++++++++++++++++++++++

---

### 2. **publicController.ts** (1286 строк)

**Проблемы:**

- ❌ **SRP**: Один контроллер обрабатывает:
  - Публичный профиль мастера
  - Бронирование слотов (сложная логика объединения клиентов)
  - Получение временных слотов
  - Управление отзывами
- ❌ **SRP**: Функция `bookPublicSlot` содержит 700+ строк сложной бизнес-логики:
  - Валидация данных
  - Поиск/создание/объединение клиентов
  - Проверка конфликтов времени
  - Создание записи
  - Отправка уведомлений
- ❌ **DIP**: Прямое использование `prisma`
- ❌ **OCP**: Сложно добавить новые типы бронирования

**Рекомендации:**

- Вынести логику бронирования в `BookingService`
- Вынести логику работы с клиентами в `ClientMergerService`
- Создать `PublicProfileService` для публичного профиля
- Создать `ReviewService` для отзывов
- Использовать репозитории для доступа к данным

---

### 3. **slotCalculator.ts** (1064 строки)

**Проблемы:**

- ❌ **SRP**: Один модуль содержит:
  - Генерацию слотов
  - Нормализацию расписания
  - Валидацию времени бронирования
  - Компрессию слотов
  - Работу с часовыми поясами
- ❌ **OCP**: Сложно добавить новые алгоритмы генерации слотов

**Рекомендации:**

- Разделить на классы:
  - `ScheduleNormalizer` - нормализация расписания
  - `SlotGenerator` - генерация слотов
  - `SlotCompressor` - компрессия слотов
  - `BookingValidator` - валидация времени бронирования
- Использовать стратегию для разных алгоритмов генерации

---

## 🟡 Средние нарушения (средний приоритет)

### 4. **authController.ts** (463 строки)

**Проблемы:**

- ❌ **SRP**: Один контроллер обрабатывает:
  - Регистрацию
  - Логин
  - Восстановление пароля (3 этапа)
- ❌ **DIP**: Прямое использование `prisma`, `jwt`
- ❌ **SRP**: Логика генерации slug должна быть в отдельном сервисе

**Рекомендации:**

- Разделить на:
  - `AuthController` - регистрация и логин
  - `PasswordResetController` - восстановление пароля
- Создать `AuthService` для бизнес-логики
- Создать `SlugGeneratorService` для генерации slug
- Использовать интерфейсы для работы с токенами

---

### 5. **servicesController.ts** (284 строки)

**Проблемы:**

- ❌ **DIP**: Прямое использование `prisma`
- ⚠️ **SRP**: Относительно хорошо, но можно улучшить

**Рекомендации:**

- Создать `ServiceRepository` для работы с БД
- Создать `ServiceService` для бизнес-логики (валидация, проверка прав)

---

### 6. **notificationService.ts** (75 строк)

**Проблемы:**

- ❌ **DIP**: Статический класс, сложно тестировать и расширять
- ❌ **OCP**: Сложно добавить новые типы уведомлений без изменения класса
- ❌ **ISP**: Класс содержит методы для всех типов уведомлений (SMS, Email, Push)

**Рекомендации:**

- Создать интерфейс `INotificationSender`
- Реализовать отдельные классы:
  - `SmsNotificationSender`
  - `EmailNotificationSender`
  - `PushNotificationSender`
- Использовать композицию в `NotificationService`

---

### 7. **notificationQueue.ts** (77 строк)

**Проблемы:**

- ❌ **DIP**: Прямая зависимость от конкретной реализации `NotificationService`
- ⚠️ **SRP**: Относительно хорошо

**Рекомендации:**

- Использовать интерфейс `INotificationService` вместо конкретного класса
- Вынести конфигурацию очереди в отдельный класс

---

## 🟢 Мелкие улучшения (низкий приоритет)

### 8. **timeUtils.ts** (497 строк)

**Проблемы:**

- ⚠️ **SRP**: Множество утилит в одном файле, но это допустимо для утилит
- Можно улучшить организацию

**Рекомендации:**

- Разделить на модули:
  - `dateUtils.ts` - работа с датами
  - `timezoneUtils.ts` - работа с часовыми поясами
  - `timeValidationUtils.ts` - валидация времени

---

### 9. **clientHelpers.ts** (36 строк)

**Проблемы:**

- ❌ **DIP**: Прямое использование `prisma`

**Рекомендации:**

- Вынести в `ClientRepository` или `ClientService`

---

## 📋 Общие архитектурные проблемы

### 10. **Отсутствие слоя репозиториев**

**Проблемы:**

- Все контроллеры напрямую используют `prisma`
- Нет абстракции для работы с БД
- Сложно тестировать и менять БД

**Рекомендации:**

- Создать интерфейсы репозиториев:
  - `IUserRepository`
  - `IAppointmentRepository`
  - `IClientRepository`
  - `IServiceRepository`
  - `IReviewRepository`
- Реализовать `PrismaUserRepository`, `PrismaAppointmentRepository` и т.д.

---

### 11. **Отсутствие слоя сервисов**

**Проблемы:**

- Бизнес-логика находится в контроллерах
- Сложно переиспользовать логику
- Сложно тестировать

**Рекомендации:**

- Создать сервисы для бизнес-логики:
  - `BookingService` - логика бронирования
  - `ClientMergerService` - объединение клиентов
  - `SlotCalculationService` - расчет слотов
  - `NotificationService` - отправка уведомлений
  - `AnalyticsService` - аналитика
  - `GeocodingService` - геокодинг

---

### 12. **Отсутствие Dependency Injection**

**Проблемы:**

- Все зависимости создаются напрямую
- Сложно тестировать
- Сложно менять реализации

**Рекомендации:**

- Внедрить DI контейнер (например, `inversify` или `tsyringe`)
- Использовать интерфейсы для всех зависимостей
- Внедрять зависимости через конструктор

---

## 📊 Приоритеты рефакторинга

### Фаза 1 (Критично):

1. ✅ Разделить `meController.ts` на отдельные контроллеры
2. ✅ Вынести бизнес-логику из `publicController.ts` в сервисы
3. ✅ Разделить `slotCalculator.ts` на отдельные классы

### Фаза 2 (Важно):

4. ✅ Создать слой репозиториев
5. ✅ Создать слой сервисов
6. ✅ Рефакторинг `authController.ts`

### Фаза 3 (Улучшения):

7. ✅ Рефакторинг `notificationService.ts` с использованием интерфейсов
8. ✅ Внедрение Dependency Injection
9. ✅ Улучшение организации утилит

---

## 📝 Примеры нарушений SOLID

### Пример 1: Нарушение SRP в `meController.ts`

```typescript
// ❌ Плохо: один контроллер делает слишком много
export async function getMe(req: Request, res: Response) {
  /* ... */
}
export async function updateProfile(req: Request, res: Response) {
  /* ... */
}
export async function getAppointments(req: Request, res: Response) {
  /* ... */
}
export async function getClients(req: Request, res: Response) {
  /* ... */
}
export async function getAnalytics(req: Request, res: Response) {
  /* ... */
}
// ... еще 10+ функций
```

### Пример 2: Нарушение DIP в контроллерах

```typescript
// ❌ Плохо: прямая зависимость от конкретной реализации
import prisma from '../prismaClient';

export async function getServices(req: Request, res: Response) {
  const services = await prisma.service.findMany({
    /* ... */
  });
}
```

```typescript
// ✅ Хорошо: зависимость от абстракции
interface IServiceRepository {
  findByMasterId(masterId: string): Promise<Service[]>;
}

export class ServiceController {
  constructor(private serviceRepo: IServiceRepository) {}

  async getServices(req: Request, res: Response) {
    const services = await this.serviceRepo.findByMasterId(req.user.id);
  }
}
```

### Пример 3: Нарушение OCP в `notificationService.ts`

```typescript
// ❌ Плохо: нужно изменять класс для добавления нового типа уведомлений
class NotificationService {
  static async sendSMS() {
    /* ... */
  }
  static async sendEmail() {
    /* ... */
  }
  static async sendPush() {
    /* ... */
  }
  // Чтобы добавить Telegram, нужно изменить класс
}
```

```typescript
// ✅ Хорошо: можно расширять через новые реализации интерфейса
interface INotificationSender {
  send(data: NotificationData): Promise<boolean>;
}

class SmsNotificationSender implements INotificationSender {
  /* ... */
}
class EmailNotificationSender implements INotificationSender {
  /* ... */
}
class TelegramNotificationSender implements INotificationSender {
  /* ... */
}
```

---

## 🎯 Метрики для оценки улучшений

После рефакторинга ожидаемые улучшения:

- ✅ Уменьшение размера контроллеров: с 2000+ строк до 100-200 строк
- ✅ Увеличение покрытия тестами: с ~0% до 70%+
- ✅ Улучшение читаемости: каждый класс отвечает за одну вещь
- ✅ Упрощение тестирования: можно мокировать зависимости
- ✅ Упрощение расширения: новые функции добавляются без изменения существующего кода

---

# 🎨 Фронтенд (React/TypeScript)

## 🔴 Критические нарушения (высокий приоритет)

### 1. **BookingWizard.tsx** (348 строк)

**Проблемы:**

- ❌ **SRP**: Компонент выполняет множество задач:
  - Загрузка данных мастера
  - Управление состоянием шагов
  - Валидация данных
  - Обработка ошибок (сложная логика с разными статусами)
  - Форматирование дат/времени
  - Навигация
  - Отправка запросов на сервер
- ❌ **DIP**: Прямая зависимость от `mastersApi`, `useNavigate`, `useSnackbar`
- ❌ **OCP**: Сложно добавить новые шаги или типы валидации

**Рекомендации:**

- Вынести логику в кастомные хуки:
  - `useMasterData` - загрузка данных мастера
  - `useBookingForm` - управление формой бронирования
  - `useBookingSubmit` - отправка формы
- Создать `BookingErrorHandler` для обработки ошибок
- Создать `DateFormatter` утилиту для форматирования дат
- Использовать контекст для управления состоянием визарда

---

### 2. **ClientsPage/index.tsx** (471 строка)

**Проблемы:**

- ❌ **SRP**: Компонент содержит:
  - Логику загрузки данных
  - Логику фильтрации (сложная с множеством полей)
  - Логику сортировки
  - Форматирование дат
  - Определение колонок таблицы
  - Управление модальными окнами
- ❌ **SRP**: Функция `filterClients` содержит сложную логику поиска по разным полям
- ❌ **DIP**: Прямая зависимость от `meApi`

**Рекомендации:**

- Вынести логику фильтрации в хук `useClientFilter`
- Вынести логику сортировки в хук `useClientSort`
- Создать утилиту `clientSearchUtils.ts` для поиска
- Вынести определение колонок в отдельный файл `clientTableColumns.tsx`
- Создать хук `useClients` для загрузки и управления клиентами

---

### 3. **api/me.ts** (564+ строк)

**Проблемы:**

- ❌ **SRP**: Один файл содержит API методы для множества сущностей:
  - Профиль мастера
  - Услуги
  - Записи
  - Клиенты
  - Аналитика
  - Портфолио
  - Расписание
  - Пароль/Email/Телефон
- ❌ **OCP**: Сложно добавить новые методы без изменения большого файла

**Рекомендации:**

- Разделить на отдельные модули:
  - `api/profile.ts` - профиль мастера
  - `api/services.ts` - услуги
  - `api/appointments.ts` - записи
  - `api/clients.ts` - клиенты
  - `api/analytics.ts` - аналитика
  - `api/portfolio.ts` - портфолио
  - `api/schedule.ts` - расписание
  - `api/account.ts` - пароль/email/телефон

---

## 🟡 Средние нарушения (средний приоритет)

### 4. **ServicesPage/index.tsx** (248 строк)

**Проблемы:**

- ❌ **SRP**: Компонент содержит:
  - Логику загрузки услуг
  - Логику создания/обновления/удаления
  - Управление модальными окнами
  - Обработку ошибок
- ❌ **DIP**: Прямая зависимость от `meApi`, `useSnackbar`

**Рекомендации:**

- Создать хук `useServices` для управления услугами
- Вынести логику CRUD операций в отдельные функции
- Использовать React Query для кеширования и синхронизации

---

### 5. **Отсутствие кастомных хуков**

**Проблемы:**

- Логика переиспользуется через копирование кода
- Сложно тестировать компоненты с встроенной логикой
- Нарушение DRY принципа

**Рекомендации:**

- Создать хуки:
  - `useClients` - управление клиентами
  - `useServices` - управление услугами
  - `useAppointments` - управление записями
  - `useMasterProfile` - профиль мастера
  - `useAnalytics` - аналитика
  - `usePortfolio` - портфолио

---

### 6. **Отсутствие сервисного слоя**

**Проблемы:**

- Бизнес-логика находится в компонентах
- Сложно переиспользовать логику
- Сложно тестировать

**Рекомендации:**

- Создать сервисы:
  - `clientService.ts` - бизнес-логика работы с клиентами
  - `bookingService.ts` - логика бронирования
  - `dateFormatter.ts` - форматирование дат
  - `errorHandler.ts` - обработка ошибок API

---

## 🟢 Мелкие улучшения (низкий приоритет)

### 7. **Отсутствие типизированных ошибок**

**Проблемы:**

- Использование `any` для обработки ошибок
- Нет единого формата ошибок

**Рекомендации:**

- Создать типы ошибок:
  ```typescript
  interface ApiError {
    status: number;
    message: string;
    details?: unknown;
  }
  ```
- Создать утилиту для обработки ошибок

---

### 8. **Дублирование логики форматирования**

**Проблемы:**

- Форматирование дат дублируется в разных компонентах
- Нет единого места для форматирования

**Рекомендации:**

- Создать утилиты:
  - `dateFormatter.ts` - форматирование дат
  - `priceFormatter.ts` - форматирование цен
  - `phoneFormatter.ts` - форматирование телефонов

---

## 📊 Приоритеты рефакторинга фронтенда

### Фаза 1 (Критично):

1. ✅ Разделить `api/me.ts` на отдельные модули
2. ✅ Вынести логику из `BookingWizard.tsx` в хуки
3. ✅ Вынести логику из `ClientsPage` в хуки

### Фаза 2 (Важно):

4. ✅ Создать кастомные хуки для переиспользования логики
5. ✅ Создать сервисный слой для бизнес-логики
6. ✅ Рефакторинг `ServicesPage`

### Фаза 3 (Улучшения):

7. ✅ Типизация ошибок
8. ✅ Создание утилит для форматирования
9. ✅ Внедрение React Query для кеширования

---

## 📝 Примеры нарушений SOLID во фронтенде

### Пример 1: Нарушение SRP в `BookingWizard.tsx`

```typescript
// ❌ Плохо: компонент делает слишком много
export const BookingWizard: React.FC<BookingWizardProps> = () => {
  // Загрузка данных
  const loadMaster = async () => { /* ... */ };

  // Управление шагами
  const [activeStep, setActiveStep] = useState(0);

  // Валидация
  if (!master || selectedServices.length === 0) { /* ... */ }

  // Форматирование дат
  const [hours, minutes] = selectedTime.split(":").map(Number);

  // Обработка ошибок (100+ строк)
  catch (err: unknown) {
    const error = err as any;
    // ... сложная логика обработки разных статусов
  }

  // Навигация
  navigate("/booking-success", { /* ... */ });
}
```

```typescript
// ✅ Хорошо: разделение ответственности
export const BookingWizard: React.FC<BookingWizardProps> = () => {
  const { master, loading } = useMasterData(masterSlug);
  const { formData, updateFormData, validateForm } = useBookingForm();
  const { submitBooking, isSubmitting } = useBookingSubmit();
  const { handleError } = useBookingErrorHandler();

  // Компонент только управляет UI
};
```

### Пример 2: Нарушение DIP в компонентах

```typescript
// ❌ Плохо: прямая зависимость от конкретной реализации
import { meApi } from '../../api/me';

export const ServicesPage: React.FC = () => {
  const loadServices = async () => {
    const data = await meApi.getServices(); // Прямая зависимость
  };
};
```

```typescript
// ✅ Хорошо: зависимость от абстракции через хук
import { useServices } from '../../hooks/useServices';

export const ServicesPage: React.FC = () => {
  const { services, loadServices, isLoading } = useServices();
  // Можно легко заменить реализацию в хуке
};
```

### Пример 3: Нарушение SRP в `api/me.ts`

```typescript
// ❌ Плохо: один объект содержит методы для всех сущностей
export const meApi = {
  getMe: async () => {
    /* ... */
  },
  updateProfile: async () => {
    /* ... */
  },
  getServices: async () => {
    /* ... */
  },
  createService: async () => {
    /* ... */
  },
  getAppointments: async () => {
    /* ... */
  },
  getClients: async () => {
    /* ... */
  },
  getAnalytics: async () => {
    /* ... */
  },
  // ... еще 20+ методов
};
```

```typescript
// ✅ Хорошо: разделение по модулям
// api/profile.ts
export const profileApi = {
  getMe: async () => {
    /* ... */
  },
  updateProfile: async () => {
    /* ... */
  },
};

// api/services.ts
export const servicesApi = {
  getServices: async () => {
    /* ... */
  },
  createService: async () => {
    /* ... */
  },
};

// api/appointments.ts
export const appointmentsApi = {
  getAppointments: async () => {
    /* ... */
  },
};
```

---

## 🎯 Метрики для оценки улучшений фронтенда

После рефакторинга ожидаемые улучшения:

- ✅ Уменьшение размера компонентов: с 400+ строк до 100-150 строк
- ✅ Увеличение переиспользования логики через хуки: с 0% до 60%+
- ✅ Улучшение тестируемости: можно тестировать хуки отдельно от компонентов
- ✅ Упрощение добавления новых функций: через новые хуки/сервисы
- ✅ Улучшение читаемости: компоненты фокусируются на UI, логика в хуках/сервисах
